import 'package:flutter/material.dart';

class AboutHomePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      body: new Column(
        children: <Widget>[
          // new GradientAppBar("Bhavani"),
          // new HomePageBody(),
        ],
      ),
    );
  }
}
